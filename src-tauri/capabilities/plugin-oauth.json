{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "plugin-oauth",
  "description": "enables the oauth:* permissions (the localhost redirect server for the Microsoft sign-in)",
  "windows": [
    "*"
  ],
  "permissions": [
    "oauth:allow-start",
    "oauth:allow-cancel"
  ]
}
