{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "plugin-dialog",
  "description": "enables the dialog:* permissions",
  "windows": [
    "main"
  ],
  "permissions": [
    "dialog:allow-ask",
    "dialog:allow-confirm",
    "dialog:allow-message",
    "dialog:allow-open",
    "dialog:allow-save"
  ]
}