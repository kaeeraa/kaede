[package]
name = "app"
version = "0.0.0"
description = "A Tauri-based Minecraft launcher written in Vue and Rust"
authors = ["windstone", "fractal"]
license = "GPL-3.0"
repository = "https://github.com/FreesmTeam/kaede"
edition = "2021"
rust-version = "1.77.2"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[lib]
name = "app_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2.4.0", features = [] }

[dependencies]
serde_json = "1.0"
serde = { version = "1.0", features = ["derive"] }
log = "0.4"
tauri = { version = "2.8.4", features = ["macos-private-api"] }
tauri-plugin-log = "2"

# Non-default installed plugins
tauri-plugin-fs = "2"
