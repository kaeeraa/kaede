// Parsed from the 'launchwrapper-of.txt' file
export type InspectedOptiFineJarType = {
  // For example, '2.3'
  "version"  : string;
  // For example, 'launchwrapper-of-2.3.jar'
  "entryPath": string;
};
